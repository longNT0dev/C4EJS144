export const sum = (a, b) => a + b
export const diff = (a, b) => a - b
export const mul = (a, b) => a * b
export const div = (a, b) => a / b

export default function printLog(fileName) {
    console.log("Đây là hàm default " + fileName)
}
